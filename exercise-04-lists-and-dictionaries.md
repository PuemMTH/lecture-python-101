---
layout: default
class: ''
---

<div class="eyebrow">IPO Gate 4 · ส่งก่อนเรียน Session 05</div>

# ด่าน 4: จัดข้อมูลนักเรียน

กำหนดข้อมูลนักเรียนเป็น list ของ dictionary อย่างน้อย 3 records แล้วเพิ่มนักเรียนใหม่ แก้คะแนน และแสดงข้อมูลก่อนกับหลังแก้ไข

<div class="grid2">
  <div class="card"><h3>ข้อกำหนด</h3><ul><li>ทุก record มี <code>id</code>, <code>name</code> และ <code>score</code></li><li>เพิ่มข้อมูลด้วย <code>append()</code></li><li>แก้และอ่านข้อมูลผ่าน index กับ key</li><li>ยังไม่ต้องใช้ loop</li></ul></div>
  <div class="card"><h3>งานที่ต้องส่ง</h3><ul><li>เขียน Input · Process · Output</li><li>วาดโครงสร้างข้อมูลเริ่มต้น 3 records</li><li>ระบุข้อมูลที่คาดหวังหลังเพิ่มและแก้ไข</li></ul></div>
</div>

<div class="lab"><p>ส่ง IPO ก่อนเขียน code · ผ่านแล้วจึงเริ่ม Session 05</p></div>
