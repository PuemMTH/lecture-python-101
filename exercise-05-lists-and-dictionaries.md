---
layout: default
class: ''
---

<div class="eyebrow">IPO Gate 05 · ส่งก่อนเรียน Session 06</div>

# ด่าน 5: จัดข้อมูลนักเรียน

กำหนดข้อมูลนักเรียนเป็น list ของ dictionary อย่างน้อย 3 records แล้วเพิ่มนักเรียนใหม่ แก้คะแนน และแสดงข้อมูลก่อนกับหลังแก้ไข

<div class="grid2">
  <div class="card"><h3>ข้อกำหนด</h3><ul><li>ทุก record มี <code>id</code>, <code>name</code> และ <code>score</code></li><li>เพิ่มข้อมูลด้วย <code>append()</code></li><li>แสดงสอง records แรกด้วย slicing</li><li>สร้าง shallow copy ก่อนแก้คะแนน</li><li>ยังไม่ต้องใช้ loop</li></ul></div>
  <div class="card"><h3>งานที่ต้องส่ง</h3><ul><li>เขียน Input · Process · Output</li><li>วาด alias/reference ก่อนและหลัง copy</li><li>ทำนายผลของ <code>students[:]</code></li><li>ระบุ object ที่ถูก mutate</li></ul></div>
</div>

<div class="lab"><p>ส่ง IPO ก่อนเขียน code · ผ่านแล้วจึงเริ่ม Session 06</p></div>
